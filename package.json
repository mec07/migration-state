{
  "name": "migration-state",
  "version": "0.1.0",
  "description": "Compute PostgreSQL schema state from migration files",
  "type": "module",
  "bin": {
    "migration-state": "src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "test": "bun test ./test/",
    "typecheck": "bunx tsc --noEmit --project tsconfig.ci.json"
  },
  "dependencies": {
    "@supabase/pg-parser": "^0.1.7",
    "commander": "^14.0.0"
  },
  "devDependencies": {
    "bun-types": "^1.3.13"
  }
}
